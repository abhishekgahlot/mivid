'use strict';

/**
 * @ngdoc function
 * @name mividApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the mividApp
 */
angular.module('mividApp')
  .controller('UserCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
